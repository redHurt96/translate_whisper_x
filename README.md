# Транскрибация видео с помощью WhisperX

Автоматическая транскрибация видео и аудио файлов с определением спикеров (диаризацией) на основе WhisperX.

## Возможности

- 🎬 Поддержка MP4, MKV, MP3 и WAV файлов
- 🗣️ Определение спикеров (диаризация)
- ⏱️ Точные временные метки
- 💾 Кэширование результатов (не нужно обрабатывать файл заново)
- 📝 Экспорт в текстовый формат
- 🌐 Веб-интерфейс на базе Gradio

## Требования

- **Python**: >= 3.11, < 3.13
- **ffmpeg**: для конвертации видео в аудио
- **Hugging Face токен**: для работы диаризации (определения спикеров)

### Установка ffmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Linux:**
```bash
sudo apt update
sudo apt install ffmpeg
```

**Windows:**
```bash
winget install ffmpeg
```

### Получение Hugging Face токена

1. Зарегистрируйтесь на [huggingface.co](https://huggingface.co)
2. Перейдите в [Settings → Access Tokens](https://huggingface.co/settings/tokens)
3. Создайте новый токен с правами на чтение
4. Примите условия использования моделей для диаризации:
   - [pyannote/speaker-diarization-3.1](https://huggingface.co/pyannote/speaker-diarization-3.1)
   - [pyannote/segmentation-3.0](https://huggingface.co/pyannote/segmentation-3.0)

## Установка

### 1. Установка зависимостей

Используйте Poetry (рекомендуется):

```bash
poetry install
poetry shell
```

Или pip:

```bash
pip install whisperx av gradio
```

### 2. Настройка токена

Токен Hugging Face настраивается через переменную окружения `HF_TOKEN`. Есть два способа:

**Способ 1: Использование `.env` файла (рекомендуется)**

Создайте файл `.env` в корне проекта:

```bash
HF_TOKEN=hf_ВАШ_ТОКЕН_ЗДЕСЬ
```

Скрипт автоматически загрузит токен из этого файла.

**Способ 2: Экспорт переменной окружения**

```bash
# macOS/Linux
export HF_TOKEN="hf_ВАШ_ТОКЕН_ЗДЕСЬ"

# Windows (PowerShell)
$env:HF_TOKEN="hf_ВАШ_ТОКЕН_ЗДЕСЬ"
```

⚠️ **Важно:** Файл `.env` уже добавлен в `.gitignore` и не будет попадать в git. Никогда не коммитьте токены в репозиторий!

## Quick start (Windows)

Автоматический установщик для Windows — работает даже на "чистом" ПК.

### Шаг 0: Установка winget (если отсутствует)

Winget — менеджер пакетов Windows, упрощает установку Python и ffmpeg.

Проверьте наличие: `winget --version`

Если winget отсутствует:
- **Windows 11:** winget предустановлен
- **Windows 10 (1809+):** установите [App Installer](https://apps.microsoft.com/detail/9NBLGGH4NNS1) из Microsoft Store
- **Альтернатива:** скачайте `.msixbundle` с [GitHub releases](https://github.com/microsoft/winget-cli/releases/latest)

> Winget необязателен — можно установить Python и ffmpeg вручную (см. ниже).

### Шаг 1: Установка Python 3.12 (если отсутствует)

Если Python 3.12 не установлен:
- **С winget:** `winget install Python.Python.3.12`
- **Без winget:** скачайте с [python.org](https://www.python.org/downloads/), при установке отметьте **"Add Python to PATH"**

### Шаг 2: Установка ffmpeg (для mp4/mkv/webm)

```bash
# С winget:
winget install ffmpeg

# Без winget:
# 1. Скачайте с https://www.gyan.dev/ffmpeg/builds/ (release build)
# 2. Распакуйте архив
# 3. Добавьте папку bin в системный PATH

# Проверка:
ffmpeg -version
```

Без ffmpeg можно работать только с mp3/wav файлами.

### Шаг 3: Запуск установщика

```
Дважды кликните setup_win.bat
```

Установщик:
- Проверит Python, pip, venv, ffmpeg
- Предложит выбрать режим: **CPU** или **GPU (NVIDIA)**
- Создаст виртуальное окружение и установит все зависимости
- Настроит файл `.env`

### Шаг 4: Добавление HF_TOKEN

Для диаризации (определения спикеров) нужен токен Hugging Face. Откройте файл `.env` и добавьте:
```
HF_TOKEN=hf_ВАШ_ТОКЕН
```

Подробнее о получении токена — в разделе "Получение Hugging Face токена".

### Шаг 5: Запуск

| Файл | Описание |
|------|----------|
| `run_ui.bat` | Запуск веб-интерфейса Gradio |
| `run_cli.bat` | Запуск командной строки |

```
Дважды кликните run_ui.bat → откроется браузер с UI
```

## Использование

### Веб-интерфейс (рекомендуется)

Запустите Gradio UI:

```bash
python ui_gradio.py
```

Откроется веб-интерфейс в браузере, где вы сможете:
- Загрузить видео/аудио файл
- Выбрать настройки (модель, язык, устройство)
- Отслеживать прогресс обработки
- Скачать готовую транскрипцию

### Командная строка (CLI)

```bash
# Базовое использование
python main.py videos/ваше-видео.mp4

# С указанием языка
python main.py videos/ваше-видео.mp4 --language ru

# С дополнительными параметрами
python main.py videos/ваше-видео.mp4 -l en --model medium --device cuda --batch-size 8
```

#### Параметры CLI

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `input_file` | Путь к видео/аудио файлу | `videos/alawar.mkv` |
| `-l, --language` | Код языка (ru, en, etc.) | Авто-определение |
| `-m, --model` | Размер модели (large-v3, medium, small, base, tiny) | `large-v3` |
| `-d, --device` | Устройство (cuda, cpu) | `cuda` |
| `--compute-type` | Тип вычислений (float16, int8, float32) | `float16` |
| `-b, --batch-size` | Размер батча | `4` |
| `--no-diarize` | Отключить определение спикеров | - |
| `--hf-token` | Токен HuggingFace | Из переменной `HF_TOKEN` |
| `-o, --output-dir` | Директория для результатов | `data` |

## Структура файлов

```
translate_whisper_x/
├── videos/                    # Сюда кладите видео/аудио
│   └── ваше-видео.mp4
├── data/                      # Результаты обработки
│   └── ваше-видео/
│       ├── audio.mp3          # Извлеченное аудио
│       ├── transcript.txt     # Готовая транскрипция
│       └── .cache/            # Промежуточные результаты
│           ├── transcribed.json   # После транскрибации
│           ├── aligned.json       # После выравнивания
│           └── diarized.json      # Финальный результат
├── core/                      # Основная логика
│   ├── __init__.py
│   ├── options.py             # Настройки пайплайна
│   ├── pipeline.py            # Пайплайн обработки
│   └── safe_globals.py        # Регистрация типов для torch
├── main.py                    # CLI интерфейс
├── ui_gradio.py               # Веб-интерфейс Gradio
├── run_ui.bat                 # Быстрый запуск UI (Windows)
├── run_cli.bat                # Быстрый запуск CLI (Windows)
└── run_ui_no_pause.bat        # Запуск UI без pause (для ярлыка)
```

## Формат вывода

Текстовый файл содержит сегменты вида:

```
[00:00:15 - 00:00:18] SPEAKER_00: Добрый день, коллеги!
[00:00:19 - 00:00:25] SPEAKER_01: Здравствуйте, давайте начнем обсуждение.
[00:00:26 - 00:00:32] SPEAKER_00: Отлично, первый вопрос по проекту...
```

## Кэширование

Скрипт сохраняет промежуточные результаты в `.cache/`:

- Если транскрибация уже выполнена — пропускает её
- Если выравнивание уже выполнено — пропускает его
- Если диаризация уже выполнена — использует готовый результат

Это **экономит время** при повторных запусках.

Чтобы **пересоздать** результат с нуля, удалите папку `.cache/`:

```bash
rm -rf data/ваше-видео/.cache/
```

Или используйте кнопку "Clear Cache" в веб-интерфейсе.

## Решение проблем

### Ошибка: `ffmpeg не найден`

Установите ffmpeg (см. раздел «Требования»).

### Ошибка при диаризации

Убедитесь, что:
1. Токен Hugging Face настроен через переменную окружения `HF_TOKEN` (в `.env` файле или экспортирован)
2. Токен корректный и имеет права на чтение
3. Вы приняли условия использования моделей pyannote (см. раздел «Получение Hugging Face токена»)

### Медленная обработка

- Используйте GPU, если доступен: `--device cuda`
- Уменьшите размер модели: `--model medium`
- Увеличьте batch_size: `--batch-size 8` (если хватает памяти)

## Лицензия

Проект использует библиотеку [WhisperX](https://github.com/m-bain/whisperX).
